# 온라인 랭킹 설정 (Firebase)

`index.html` 의 `FB_CONFIG` 를 채우면 온라인 랭킹이 켜지고, `null` 로 두면 예전처럼
로컬(이 브라우저) 전용으로만 동작합니다. 게임 자체는 설정 없이도 그대로 돌아갑니다.

## 1. Firebase 프로젝트 만들기

1. https://console.firebase.google.com 에서 프로젝트 생성
2. **빌드 → Firestore Database → 데이터베이스 만들기** (프로덕션 모드, 지역은 `asia-northeast3` 서울 권장)
3. **빌드 → Authentication → 시작하기 → 익명(Anonymous) 사용 설정**
   - 게임 계정은 기존처럼 로컬에 남고, 익명 인증은 "이 기기의 기록 소유자"를 구분하는 용도로만 씁니다.
4. **프로젝트 설정 → 내 앱 → 웹 앱 추가** → 나오는 `firebaseConfig` 객체를 복사

## 2. index.html 에 설정 넣기

`index.html` 에서 `const FB_CONFIG=null;` 을 찾아 복사한 값으로 바꿉니다.

```js
const FB_CONFIG={
  apiKey:"…",
  authDomain:"프로젝트.firebaseapp.com",
  projectId:"프로젝트",
  appId:"1:…:web:…"
};
```

> `apiKey` 는 비밀번호가 아니라 프로젝트 식별자라서 공개돼도 됩니다.
> 실제 보호는 아래 보안 규칙이 합니다.

## 3. 보안 규칙 적용 (이게 실제 방어선입니다)

**Firestore Database → 규칙** 탭에 이 저장소의 `firestore.rules` 내용을 붙여넣고 게시합니다.

규칙이 거부하는 것:

| 검사 | 내용 |
|---|---|
| 소유권 | 자기 `uid` 문서에만 쓸 수 있음 |
| 필드 | 정해진 8개 필드·타입만 허용, 음수 불가 |
| 스테이지 | 500 초과 거부 |
| 처치 수 | 스테이지 − 1 미만이면 거부 |
| 환생 | `stage >= 12 + 4×(환생−1)` 조건 미달이면 거부 |
| DPS | 스테이지 대비 상한(`1.55^(stage+25)×100`) 초과 거부 |
| 골드 | 스테이지 대비 상한 초과 거부 |
| RP | 스테이지·환생분보다 작거나 +20000 넘게 부풀리면 거부 |
| 시각 | 서버 시간과 크게 어긋나면 거부 |
| 빈도 | 30초에 한 번 초과 쓰기 거부 |
| 삭제 | 전면 금지 |

**게시 전에 콘솔의 "규칙 플레이그라운드"에서 한 번 시뮬레이션해 보세요.**
특히 `math.pow` 가 거부되면 해당 두 줄(DPS·골드 상한)을 지우고 나머지만 써도
대부분의 조작은 걸러집니다.

## 4. 인덱스

`rp` 내림차순 단일 필드 정렬만 쓰므로 별도 복합 인덱스는 필요 없습니다.

## 5. 배포 시 주의

- Firebase SDK 를 CDN에서 `import()` 로 받아오므로 **`file://` 로 열면 동작하지 않습니다.**
  GitHub Pages 등 `http(s)` 로 서빙해야 합니다. (로컬 확인은 `python -m http.server`)
- **승인된 도메인**: Authentication → 설정 → 승인된 도메인에 배포 도메인
  (예: `seowls734.github.io`)을 추가해야 익명 로그인이 통과합니다.
- 무료(Spark) 요금제 기준 하루 읽기 5만 / 쓰기 2만 건입니다. 클라이언트는
  60초에 한 번, 그것도 기록이 갱신됐을 때만 씁니다. 읽기는 실시간 구독이라
  접속자가 많아지면 읽기 쪽이 먼저 찹니다 — 부담되면 `index.html` 의
  `FB_BOARD_LIMIT` 를 줄이거나 `onSnapshot` 을 `getDocs` 1회 조회로 바꾸세요.

## 남는 한계

규칙은 **"불가능한 값"** 만 거부합니다. 스테이지·DPS·환생을 서로 앞뒤 맞게 조작한
기록은 통과합니다. 그걸 막으려면 플레이 시간·성장 곡선을 서버가 추적해야 하고,
Cloud Functions 로 기록을 검증하는 구조가 필요합니다. 지금 단계에서는
"순위표가 말이 되게 유지하는" 수준으로 보시면 됩니다.
